# Engine module environment variables
# Copy to engine_module/.env (local, do not commit)

# Risk and trading defaults
MAX_POSITION_SIZE_PCT=5.0
MAX_LEVERAGE=2.0
MAX_CONCURRENT_TRADES=3
DAILY_LOSS_LIMIT_PCT=2.0
DEFAULT_STOP_LOSS_PCT=1.5
DEFAULT_TAKE_PROFIT_PCT=3.0
ORDER_TIMEOUT_SECONDS=30

# Redis connection (if engine uses it)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
